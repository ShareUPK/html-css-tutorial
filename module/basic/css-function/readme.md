<!---

    16 ♥ - CSS Functions
    - var()
    - linear-gradient()
    - rgba() : rgb(red, green, blue, alpha) : alpha => độ mờ của nền (opacity)
    - rgb()  : rgb(red, green, blue)
    - calc() : calc(px + px) : (+, -, *, / => Kích thước của khung)
    - attr() :
    
--->